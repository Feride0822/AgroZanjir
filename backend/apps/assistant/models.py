"""No tables.

The assistant is stateless on purpose. A visitor's question and the transcript
it belongs to are held by their browser for the length of the conversation and
by nothing else: the public website asks for no account, and a table of what
anonymous people typed into a box would be a data-protection liability the
product gets nothing back for. What the assistant reads is already public;
what it is asked is nobody's business.

`apps/panels` has the same empty models module for the same kind of reason.
"""
