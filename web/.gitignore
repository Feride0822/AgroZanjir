node_modules/
dist/
.env.local
*.local

# TypeScript's incremental build cache. Machine-specific, rewritten by every
# `tsc -b`, and tracked here since the original import - which meant every
# build produced a diff.
*.tsbuildinfo
